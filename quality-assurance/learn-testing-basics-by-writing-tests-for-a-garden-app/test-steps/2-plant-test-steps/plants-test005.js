import { Plant } from "../src/Plant";
const chai = require("chai");
const assert = chai.assert;

describe("Plant Tests", function () {

});

// Inside the `describe` block, declare a variable `myPlant` and assign it to a new `Plant` with `"Sunflower"` as a parameter.