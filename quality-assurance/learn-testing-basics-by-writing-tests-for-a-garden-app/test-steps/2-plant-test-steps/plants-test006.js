import { Plant } from "../src/Plant";
const chai = require("chai");
const assert = chai.assert;

describe("Plant Tests", function () {
    let myPlant = new Plant("Sunflower");
});

// Great! Let's add a `beforeEach` hook to set up the `myPlant` variable for each test case in the `"Plant Tests"` tests.