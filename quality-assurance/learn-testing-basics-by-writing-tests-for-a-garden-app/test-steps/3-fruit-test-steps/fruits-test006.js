import { FruitTree } from "../src/FruitTree";
const chai = require("chai");
const assert = chai.assert;

describe("Fruit Tree Tests", function () {
    let myFruitTree = new FruitTree("Strawberry");
});

// Great! Let's add a `beforeEach` hook to set up the `myFruitTree` variable for each test case in the `"Fruit Tree Tests"` tests.