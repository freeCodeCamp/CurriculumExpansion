import { VegetablePlant } from "../src/VegetablePlant";
const chai = require("chai");
const assert = chai.assert;

describe("Vegetable Plant Tests", function () {

});

// Inside the `describe` block, declare a variable `myVegetablePlant` and assign it to a new `VegetablePlant` with `"Cabbage"` as a parameter.