// Create a PORT variable and assign it to 3000, this will be the port your server will listen to
// Call the app's `listen` method passing a PORT variable

const express = require("express");
const app = express();
