/*
First create a `getUpperCase()` function which takes a string and returns an uppercase version of the string.
For example, here is a getTrimmed() function which takes a string and returns it with no whitespace at the beginning or end: `const getTrimmed = (str) => str.trim();`
*/
