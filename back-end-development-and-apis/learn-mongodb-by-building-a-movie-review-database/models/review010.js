const mongoose = require('mongoose');

const Schema = mongoose.Schema;

/*
Now create a new empty schema called "reviewSchema" to define the shape of the review documents.

Here is an empty schema called "exerciseSchema":
```
const exerciseSchema = new Schema({
});
```
*/