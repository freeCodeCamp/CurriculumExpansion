// Normally you would install Express with a package manager like npm or yarn, but we've already taken care of that for you.
// Import Express into your project by using the `require` keyword.
// Here is an example of using `require` to import the Path module: `const path = require('path');`.
