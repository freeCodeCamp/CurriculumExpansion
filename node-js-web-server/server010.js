const http = require('http');

// To create a simple server, use the `http` module's `.createServer()` method.
// For example, the code below uses the `.readFile()` method from the `fs` module:
// ```
// const fs = require ('fs');
// fs.readFile()
// ```