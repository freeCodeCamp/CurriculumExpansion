const newsFeed = {
    page: "front_page"
}

/*
We now have our 'front_page' data property for our newsfeed.

An Object may also contain an array or another object.  For Example: var objectExample = {cats:[]}. *The property cats of objectExample is set
to an empty array. 

After the property page, create a new articles property and set it to an empty array. 
*/

