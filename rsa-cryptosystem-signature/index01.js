/*
Digital signature is a scheme to verify authenticity and maintain integrity of digital documents and messages.

First, we hash our message. It is a method to transform a document into a fixed-size value.

Create a hash function `hashTheMessage()` with `message` as a parameter. 
*/
