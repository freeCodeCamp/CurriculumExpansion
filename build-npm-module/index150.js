const getUpperCase = (str) => str.toUpperCase();

const getLowerCase = (str) => str.toLowerCase();

/*
Create a `getSentenceCase()` function which takes a string returns a sentence case version of it.
Just write `getSentenceCase()` as an empty function with the parameter `str` for now.
For example, here is an empty function with `arr` as the parameter: `const getArrLength = (arr) => { }`
*/
