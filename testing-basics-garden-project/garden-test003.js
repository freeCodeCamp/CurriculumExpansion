import { Garden } from '../src/Garden';
const chai = require("chai");

/* We are going to use the command "assert" a lot throughout this project.
   To use chai's assert, you'd have to call chai.assert.
   Typing that for every test would be tedious, so we won't do that.
   Instead, create a variable called "assert" and set it to chai's assert command.
*/