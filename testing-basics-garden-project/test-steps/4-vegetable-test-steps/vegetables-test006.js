import { VegetablePlant } from "../src/VegetablePlant";
const chai = require("chai");
const assert = chai.assert;

describe("Vegetable Plant Tests", function () {
    let myVegetablePlant = new VegetablePlant("Cabbage");
});

// Great! Let's add a `beforeEach` hook to set up the `myVegetablePlant` variable for each test case in the `"Vegetable Plant Tests"` tests.