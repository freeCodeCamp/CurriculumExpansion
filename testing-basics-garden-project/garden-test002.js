import { Garden } from '../src/Garden';

/* Often, code is tested against assert libraries.
   Javscript has its own assert funcitons that you can look into.
   For this project, we're going to use the chai assert library.
   Require the "chai" module in your code and set it to variable "chai"
*/